num_workers: 10
batch_size: 5
cuda: 1
weight_name: "change_miou" # Used during resume, select with model to load from [miou, macc, acc, miou_ch..., latest]
enable_cudnn: True
checkpoint_dir: "/home/user/zhanwenxiao/instancesegmentation/code/instance_changedetection/outputs/SiamKPConv-2025-11-28/SiamKPConv-2025-11-28-SiamEncFusionSkipKPConv-20251128_192925"
model_name: SiamEncFusionSkipKPConv # SiameseKPConvUnshared SiameseKPConv
precompute_multi_scale: False # Compute multiscate features on cpu for faster training / inference
enable_dropout: False
voting_runs: 1

tracker_options: # Extra options for the tracker
  full_pc: True
  full_res: True
  save_pc: True
  name_test: ""

hydra:
  run:
    dir: ${checkpoint_dir}/eval/${now:%Y-%m-%d_%H-%M-%S}
